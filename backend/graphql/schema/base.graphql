scalar Upload
scalar Time
scalar Duration

directive @binding(constraint: String!) on INPUT_FIELD_DEFINITION | ARGUMENT_DEFINITION

enum OrderDirection {
    ASC
    DESC
}

type Query {
    version: String!
}

type Mutation {
    version: String!
}
